apiVersion: apps/v1
kind: Deployment
metadata:
  name: goserver
  namespace: default
  labels:
    app: goserver
spec:
  replicas: 2
  selector:
    matchLabels:
      app: goserver
  template:
    metadata:
      name: "goserver"
      namespace: default
      labels:
        app: "goserver"
    spec:
      containers:
      - name: "goserver"
        image: "wander4747/hello-k8s:latest"